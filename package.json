{
  "name": "enum",
  "version": "1.0.0",
  "description": "Add simple enumerates (copy,clone,each and extend) to the config hash.",
  "main": "enum.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@git.geointapps.org:geoproto/enum"
  },
  "author": "ACME SDS",
  "license": "ISC"
}
